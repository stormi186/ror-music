<tr>
  <td><%= link_to genre.name, genre_path(genre) %></td>
  <td>
  <div class="btn-group">
    <%= link_to 'Edit', edit_genre_path(genre), class: 'btn btn-sm btn-primary' %>
    <%= link_to 'Delete', genre_path(genre), method: :delete, data: {confirm: "Are you sure?"}, class: 'btn btn-sm btn-danger' %>
    </div>
  </td>
</tr>
